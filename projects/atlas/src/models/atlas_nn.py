"""atlas_nn reproduction — STUB. Implement build_model() + register it."""
from . import register

@register("atlas_nn")
def build_model(cfg, **kw):
    raise NotImplementedError("Confirm atlas target, then implement.")
