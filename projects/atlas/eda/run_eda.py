"""atlas EDA — STUB. Copy the shape of genre/eda/run_eda.py and adapt to this domain.
Must emit data/<phase>/eda_stats.json (schema eda_stats/1.x) + eda/figures/<phase>/*.png.
Keep the eda_stats.json keys the dashboard reads: missing_corrupt, type_audit,
nerd_stats, figures. Domain-specific blocks are fine to add.
"""
def main():
    raise NotImplementedError("Implement atlas EDA — see projects/genre/eda/run_eda.py")

if __name__ == "__main__":
    main()
